package org.jetbrains.kotlin.idea.formatter;

import com.intellij.application.options.CodeStyleAbstractPanel;
import com.intellij.psi.codeStyle.CodeStyleSettings;

/** Compile-time stub for excluded KotlinCodeStylePanel.kt. */
public class KotlinCodeStylePanel extends CodeStyleAbstractPanel {
    public KotlinCodeStylePanel(CodeStyleSettings currentSettings, CodeStyleSettings settings) {
        super(settings);
    }
}
